import { FC } from "react";
import Header from "../components/Header";

const Main: FC = () => {
  return (
    <div>
      <Header />
      <p>Авторизован</p>
    </div>
  );
};

export default Main;
