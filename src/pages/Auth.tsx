import { FC } from "react";
import Header from "../components/Header";
import Form from "../components/Form";

const Auth: FC = () => {
  return (
    <div>
      <Header />
      <Form />
    </div>
  );
};

export default Auth;
